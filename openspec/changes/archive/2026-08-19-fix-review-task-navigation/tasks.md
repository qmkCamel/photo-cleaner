## 1. 精确任务导航

- [x] 1.1 为 `CleanupFlowState` 增加按唯一分组 ID 选择入口，失效 ID 不修改当前状态。
- [x] 1.2 将首页路由和任务自动化标识切换为唯一任务 ID，确保同分类任务不再落到第一组。

## 2. 首页预览表达

- [x] 2.1 为共享缩略图增加可关闭选择指示器的展示参数，仅在首页任务预览中关闭。
- [x] 2.2 保持复核详情的真实选择、推荐保留和保守默认值不变。

## 3. 自动化与验证

- [x] 3.1 增加同分类多分组单元测试，覆盖第二组精确选择和失效 ID 安全失败。
- [x] 3.2 增加禁用真实删除的多相似组 XCUITest，保留首页无圆框和第二组详情命名截图。
- [x] 3.3 运行聚焦单元测试、UI 测试、模拟器构建和 `git diff --check`，记录实际结果。
  - `TrueKeepTests`：75/75 通过。
  - 受影响 UI 回归：4/4 通过；保留首页结果态和第二组详情态截图。
  - iPhone 17 模拟器构建通过；`git diff --check` 通过。
- [x] 3.4 运行 OpenSpec 状态、严格校验和生命周期检查，记录实际结果。
  - 变更状态完整；严格校验与生命周期检查均通过。
