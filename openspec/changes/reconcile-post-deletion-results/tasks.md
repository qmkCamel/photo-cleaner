## 1. 状态模型与协调

- [x] 1.1 为当前扫描周期增加结构化删除摘要，记录去重后的成功资源 ID、成功数量和候选预计体积，并替代可漂移的布尔成功状态。
- [x] 1.2 扩展 `CleanupFlowState.applyDeletionResult`，按 Photos 实际成功 ID 原子更新复核箱、当前选择、复核分组、任务、缩略图、预计体积和当前分组索引。
- [x] 1.3 提取扫描构建与删除协调共用的任务重建纯函数，并确保只剩推荐保留项的分组不会继续生成待复核任务。
- [x] 1.4 在新扫描开始时清空删除摘要和旧派生状态，同时继续复用现有扫描进度、取消、中断和完成路径。

## 2. 删除后用户界面

- [x] 2.1 在首页增加“已移至‘最近删除’”摘要，展示累计成功数量和预计体积，并明确项目仍可恢复、空间可能尚未立即释放。
- [x] 2.2 在全部处理完成时隐藏旧空间指标和任务卡，展示“本轮没有待复核项目”空态并保留“重新扫描”。
- [x] 2.3 在仍有候选时展示“剩余预计可释放空间”，同步更新任务数量、缩略图和体积，并在 Limited Photos Access 下保留范围提醒。
- [x] 2.4 保持部分失败项目在复核箱中选中，展示失败原因、重试和恢复路径；全部失败时不得改变首页结果或展示成功摘要。
- [x] 2.5 为成功、部分成功和失败状态补充 VoiceOver 状态播报、可理解标签、非纯颜色反馈和 Dynamic Type 重排。

## 3. 安全回归覆盖

- [x] 3.1 增加状态单元测试，覆盖全成功、有剩余、全部清空、相似组只剩推荐保留项、多批累计、部分成功、全部失败和重新扫描重置。
- [x] 3.2 增加界面测试，断言删除成功后旧候选、旧缩略图和旧空间不会继续出现在首页，并覆盖 Limited Access 与大号 Dynamic Type。
- [x] 3.3 使用假删除服务或确定性测试场景更新 AppClaw/UI 自动化；测试必须继续启用真实删除安全锁。实际结果：新增 `post-deletion-home-sync` P0 Flow，模拟器 AppClaw 严格模式 12/12 步通过，且双重禁删保护保持启用。
- [ ] 3.4 经明确批准后使用一张可恢复测试照片执行真机破坏性 smoke，验证首页同步、“最近删除”恢复窗口和 iCloud Photos 风险提示，并立即恢复测试照片。

## 4. 验证与交付

- [x] 4.1 运行 `openspec validate reconcile-post-deletion-results --type change --strict --no-interactive`。实际结果：通过，proposal、design、specs 和 tasks 四类制品完整且 change valid。
- [x] 4.2 运行聚焦 Swift 单元测试和 UI 测试，记录成功、部分成功、失败与无障碍场景的实际结果。实际结果：状态、权限和标识符聚焦测试 27/27 通过；删除后首页与超大字号 UI 聚焦用例均通过；超大字号保存布局截图并执行除 iOS 26.3 无目标元素误报项外的无障碍审计。
- [x] 4.3 运行仓库完整 Xcode 回归、`python3 iOS/TrueKeep/Scripts/release_preflight.py --run-xcode` 和 `python3 openspec/check.py`，并记录实际结果。实际结果：完整 Xcode 90/90 通过；发布前检查 15 项通过/信息、0 失败，Release 无签名归档成功；OpenSpec 严格校验与生命周期检查通过。
- [x] 4.4 运行 `git diff --check`。实际结果：通过；实现、测试、AppClaw Flow 与 OpenSpec 文档均无空白错误。
