## 1. 完整构图与分析策略

- [x] 1.1 将粗筛图片请求改为 `224px aspectFit`，保留快速本地资源优先、高质量本地资源回退和禁止网络访问选项。
- [x] 1.2 实现方向规范化和保持宽高比的分析栅格，确保填充区不进入亮度、饱和度与清晰度指标，并为 pHash 增加宽高比可比较性约束。
- [x] 1.3 增加横图、竖图、全景图、方向元数据和不同宽高比回退比较的聚焦单元测试。

## 2. 两阶段候选复核

- [x] 2.1 引入粗筛证据、复核证据和最终分类模型，生成只包含相似组成员、模糊/误拍命中项及配置边界项的复核集合。
- [x] 2.2 对复核集合请求约 `512px aspectFit` 本地图片，重新计算 Feature Print、清晰度、Vision 图片质量和可用的人脸拍摄质量。
- [x] 2.3 将粗筛、复核阈值和最终风险合并规则集中到可注入策略，使用可控 fixture 标定新栅格与不同分辨率下的阈值。
- [x] 2.4 使用复核证据重新聚类、拆组和排序，只从最终成员生成稳定分组 ID，并保守处理复核资源缺失的候选。
- [x] 2.5 增加无候选不请求 512、相似组全员复核、错误组拆分、推荐保留重排、模糊/误拍撤销和复核缺失降级测试。

## 3. 精确重复确认

- [x] 3.1 实现底层照片资源选择器和禁止网络访问的流式 SHA-256 摘要，只处理已高度相似的候选子集。
- [x] 3.2 为普通照片和 Live Photo 定义可比较资源集合；Live Photo 必须同时确认主图片与配对视频，证据不完整时不得升级为精确重复。
- [x] 3.3 增加相同资源、重新编码但画面相似、资源类型不同、iCloud-only 资源、取消和分块摘要的单元测试。

## 4. 扫描阶段与恢复

- [x] 4.1 扩展 `PhotoLibraryScanning` 进度通道，报告快速分析、候选复核和结果整理的真实阶段与局部计数。
- [x] 4.2 由 `AppRootView` 按 active scan ID 接收进度并只发布最终结果；扫描状态独立于当前页面，离开进度页后继续扫描且首页保留动作局部状态和返回入口。
- [x] 4.3 取消时撤销可取消请求并丢弃本轮瞬时证据，保留被中断轮次的扫描范围用于重试。
- [x] 4.4 更新 `ScanProgressView` 和首页扫描状态的阶段文案、VoiceOver、Dynamic Type 与自动化标识。
- [x] 4.5 增加候选复核取消、离页继续扫描、旧 scan ID 更新隔离、无部分结果发布和中断重试的状态/UI 测试。

## 5. 性能与自动化回归

- [ ] 5.1 为粗筛、复核和资源摘要设置小批量/有限并发与缓存释放边界，记录普通和低电量策略下的候选数、峰值内存和扫描耗时。
- [x] 5.2 使用关闭真实删除的模拟器真实 Photos 资产运行 AppClaw，验证横竖图相似组、推荐保留和精确重复结果，并保留“快速分析”“候选复核”“扫描结果”命名截图及 step report。
- [x] 5.3 覆盖复核资源不可用或取消恢复路径时，保留对应低置信度/中断状态命名截图；未执行的失败路径不得伪造截图。
- [x] 5.4 运行聚焦 Swift 测试、完整单元/UI 回归和模拟器构建，记录实际通过项与既有基线失败。
- [x] 5.5 运行 `openspec status --change refine-photo-analysis-pipeline --json`、严格 OpenSpec 校验、`python3 openspec/check.py` 和 `git diff --check`，记录实际结果。

## 验证记录

- 2026-08-20：聚焦单元测试 90/90 通过；完整回归 90 个单元测试和 28 个 UI 测试全部通过，结果包为 `/tmp/truekeep-full-refine-20260820-1.xcresult`。
- 2026-08-20：AppClaw P0 回归 15/15 通过，报告为 `.appclaw/runs/20260820T081416-bfc1f7/manifest.json`；候选复核离页继续和取消恢复的 XCUITest 命名截图位于 `.appclaw/artifacts/refine-photo-analysis-pipeline/2026-08-20-xcuitest/`。
- 2026-08-20：关闭真实删除后，使用模拟器真实 Photos 资产运行 AppClaw 12/12 通过，报告为 `.appclaw/runs/20260820T085020-6e792a/manifest.json`。真实资产包含横图和竖图字节级副本，结果确认两个相似组、精确重复语义及推荐保留；快速分析和扫描结果截图位于 `.appclaw/artifacts/refine-photo-analysis-pipeline/2026-08-20-real-photo-library-appclaw/`，精确重复结果截图位于 `.appclaw/artifacts/refine-photo-analysis-pipeline/2026-08-20-real-photo-library-xcuitest/`。候选复核命名截图由确定性 AppClaw/XCUITest 场景保留，避免依赖小相册中不可稳定截取的瞬时阶段。
- 2026-08-20：`openspec status` 返回所有制品完成；严格校验通过；`python3 openspec/check.py` 8/8 通过；`git diff --check` 通过。
- 5.1 尚未完成：实现已保持顺序有界处理、批次释放和低电量分类上限，但还没有记录普通/低电量两种策略下的峰值内存与完整扫描耗时，因此不勾选。
