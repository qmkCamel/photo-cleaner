## 1. 本地图片请求策略

- [x] 1.1 增加快速资源优先、高质量本地资源回退的有序请求策略，两个请求都必须禁止网络访问。
- [x] 1.2 将视觉指标生成接入回退策略，保持分类上限、截图跳过、取消检查和单资源失败隔离。

## 2. 自动化覆盖

- [x] 2.1 增加单元测试，覆盖快速请求成功短路、快速请求失败后高质量回退、两次失败和固定请求顺序。
- [x] 2.2 使用禁用真实删除的 AppClaw 模拟器流程扫描真实 Photos 资产，验证相似任务恢复，并保留扫描完成与结果页命名截图。

## 3. 验证与交付

- [x] 3.1 运行 OpenSpec 状态检查和严格校验，记录实际结果。
- [ ] 3.2 运行聚焦 Swift 单元测试、完整 Xcode 回归和构建检查，记录实际结果。
  - 聚焦测试 24/24、完整单元测试 73/73、模拟器构建通过。
  - 完整 Xcode 回归 98/99；唯一失败为与本次 PhotoKit 回退无关的既有日期范围结果页对比度审计，单独复跑仍失败。
- [x] 3.3 运行 `git diff --check` 和 `python3 openspec/check.py`，区分本变更结果与已有活跃变更基线。
